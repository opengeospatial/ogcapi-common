[[rm_scale]]
[cols="1,4",width="90%"]
|===
2+|*Requirements Module*
2+|http://www.opengis.net/spec/ogcapi-common-2/1.0/rm/scale
|Target type |Web API Query Parameter
|===

The `scale` parameter is used to increase or decrease the scale of a geospatial resource.

The `scale` parameter is defined as follows:

include::requirements/REQ_scale-definition.adoc[]

While the processing of the `scale` parameter is specific to the resource and operation for which it is applied, there is a general set of requirements which all implementations must address.

include::requirements/REQ_scale-success.adoc[]

=== Subsetting Examples

include::examples/examples_scale.adoc[]
